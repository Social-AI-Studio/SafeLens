# =============================
# Auth Service Environment File
# =============================
#
# How to set the DOMAIN for this service
# - Use the full public URL of this auth service INCLUDING the /api/auth path
# - Must include scheme (http/https)
# - No trailing slash after /api/auth
#
# Examples:
#   Local dev  : http://localhost:3001/api/auth
#   Production : https://auth.yourdomain.com/api/auth

# URL used by the browser (exposed to client code)
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3001/api/auth

# URL used by the server (issuer/audience, callbacks, etc.)
AUTH_BASE_URL=http://localhost:3001/api/auth

# PostgreSQL for the auth service (adjust user/password/port/db)
DATABASE_URL=postgres://user:password@localhost:5433/auth_service

# Google OAuth Credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Better Auth secret (required). Generate with:  openssl rand -hex 32
BETTER_AUTH_SECRET=

# --- Production examples (uncomment and edit) ---
# NEXT_PUBLIC_BETTER_AUTH_URL=https://auth.yourdomain.com/api/auth
# AUTH_BASE_URL=https://auth.yourdomain.com/api/auth
